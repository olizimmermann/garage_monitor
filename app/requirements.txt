# Pinned to the versions validated in the published Docker image for
# reproducible builds. Bump deliberately and re-test against a real RTSP stream.
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pydantic==2.13.4
opencv-python-headless==4.13.0.92
scikit-image==0.26.0
paho-mqtt==2.1.0
